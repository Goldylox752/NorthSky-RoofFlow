js id="q2iyb9" const Stripe = require("stripe");  const requiredEnv = (key) => {   const value = process.env[key]?.trim();    if (!value) {     throw new Error(`Missing required env: ${key}`);   }    return value; };  const stripe = new Stripe(   requiredEnv("STRIPE_SECRET_KEY"),   {     apiVersion: "2025-04-30.basil",     maxNetworkRetries: 2,     timeout: 30000,   } );  module.exports = stripe; 