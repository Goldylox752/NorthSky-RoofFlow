js id="o6u6e6" import twilio from "twilio";  const requiredEnv = (key) => {   const value = process.env[key]?.trim();    if (!value) {     throw new Error(       `Missing environment variable: ${key}`     );   }    return value; };  class TwilioService {   static instance;    static getClient() {     if (!TwilioService.instance) {       TwilioService.instance = twilio(         requiredEnv("TWILIO_SID"),         requiredEnv("TWILIO_AUTH_TOKEN"),         {           lazyLoading: true,         }       );        console.log(         "📞 Twilio client ready"       );     }      return TwilioService.instance;   } }  export const client =   TwilioService.getClient(); 